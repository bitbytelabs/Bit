{
  "name": "bit-maintainer-bot",
  "version": "1.0.0",
  "private": true,
  "description": "Probot app to help maintain the Bit repository",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "probot": "^14.0.0"
  },
  "devDependencies": {
    "vitest": "^4.0.18"
  }
}
